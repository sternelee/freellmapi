name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Run server tests
        run: pnpm --filter @freellmapi/server test

      - name: Build server
        run: pnpm --filter @freellmapi/server build

      - name: Build client
        run: pnpm --filter @freellmapi/client build
